hoeringsportal_openid_connect.redirect_controller.authorize:
  path: '/hoeringsportal-openid-connect/authorize/{client_id}'
  defaults:
    _controller: '\Drupal\hoeringsportal_openid_connect\Controller\OpenIDConnectRedirectController::authorize'
    _title: 'Høringsportal OpenID Connect authorize'
  requirements:
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'

hoeringsportal_openid_connect.redirect_controller.authenticate:
  path: '/hoeringsportal-openid-connect/authenticate/{client_id}'
  defaults:
    _controller: '\Drupal\hoeringsportal_openid_connect\Controller\OpenIDConnectRedirectController::authenticate'
    _title: 'Høringsportal OpenID Connect authenticate'
  requirements:
    _access: 'TRUE'
  options:
    no_cache: 'TRUE'
